global:
  scrape_interval: 1s
  evaluation_interval: 5s

scrape_configs:
  - job_name: threshold
    static_configs:
      - targets: ["exporter:9109"]
        labels:
          site: gainesville-enclosure-1
