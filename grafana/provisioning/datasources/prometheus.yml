apiVersion: 1

datasources:
  - name: THRESHOLD Prometheus
    uid: threshold-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 1s
      httpMethod: POST
